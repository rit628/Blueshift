int main() {
    println("in");
    int i = 1;
    i = i - 2;
    
    if (i > 0) {
        println("works", true);
    }
    else if (i == i) {
        println("elif works");
    }
    else {
        return false;
    }

    int j = 2;

    println(j);

    j+=2;

    println(j);

    //continue;

    //break;

    for (int i = 0; i < 10; ++i) {
        println(i);
    }
/*
    while (i < 10) {
        println(i);
        if (i == 8) {
            break;
        }
        i = i + 1;
    }*/

    list<int> a = [1, 2, 4];

    println("past assignment");

    println("a[2]", a[2]);

    map<int> def = {"testing" : 2, "sfml" : 10};

    println("def[\"sfml\"]", def["sfml"]);

    return 10.0 % 3.0;
}

oblock doSomething(LIGHT a) {
    a.state = !a.state;
}

setup() {
    LIGHT abc = "CTL1::GND-12,VIN-1,GPIO-13,VOUT-2";
    doSomething(abc);
    //main();
}